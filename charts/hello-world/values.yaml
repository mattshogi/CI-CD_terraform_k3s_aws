replicaCount: 1

image:
  # The image built and pushed by this repo's CI pipeline. Real deploys always
  # override the tag with the immutable sha-<commit> tag of the build being
  # deployed; "main" is only the fallback for ad-hoc local installs.
  repository: ghcr.io/mattshogi/ci-cd_terraform_k3s_aws/hello-world
  tag: "main"
  pullPolicy: IfNotPresent

# Extra container args. Empty for the Go app; the bootstrap fallback image
# (hashicorp/http-echo) sets ["-text=..."] here.
args: []

containerPort: 5678

service:
  # NodePort so the demo endpoint is reachable without a cloud LoadBalancer
  # (single-node k3s on EC2). Set to ClusterIP when fronting with ingress only.
  type: NodePort
  port: 80
  nodePort: 30080

ingress:
  enabled: true
  className: traefik
  annotations: {}
  path: /
  # Empty host = match any host (demo is accessed by raw public IP)
  host: ""
  tls: []

probes:
  # The Go app exposes /healthz; override to "/" for images without it.
  path: /healthz

# Spread replicas across nodes (HA mode sets replicaCount>1 across 3 servers).
# whenUnsatisfiable=ScheduleAnyway keeps it soft so single-node clusters still
# schedule every replica instead of leaving pods Pending.
topologySpread:
  enabled: true
  maxSkew: 1
  topologyKey: kubernetes.io/hostname
  whenUnsatisfiable: ScheduleAnyway

# PodDisruptionBudget is rendered only when replicaCount > 1 (see templates/pdb.yaml).
# minAvailable keeps at least this many pods up during voluntary disruptions.
pdb:
  minAvailable: 1

resources:
  requests:
    cpu: 5m
    memory: 16Mi
  limits:
    memory: 48Mi

podSecurityContext:
  runAsNonRoot: true
  runAsUser: 65532
  runAsGroup: 65532
  seccompProfile:
    type: RuntimeDefault

securityContext:
  allowPrivilegeEscalation: false
  readOnlyRootFilesystem: true
  capabilities:
    drop: ["ALL"]

nameOverride: ""
fullnameOverride: ""
